<div>
    Pinboard Header -
    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
        Hi {{ app.getUser().username }} - <a class="link" href="{{ url('logout') }}">Logout</a>
    {% else %}
        <a href="{{ url('login') }}">Login</a>
    {% endif %}
</div>